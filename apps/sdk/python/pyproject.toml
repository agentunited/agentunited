[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "agentunited"
version = "0.1.0"
description = "Agent United Python SDK"
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["requests>=2.31.0"]
